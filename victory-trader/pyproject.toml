[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "victory-trader"
version = "0.1.0"
description = "Research platform for short-horizon momentum in low-priced U.S. equities"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.32",
  "python-dotenv>=1.0",
  "pydantic>=2.8",
  "pandas>=2.2",
  "polars>=1.5",
  "pyarrow>=17",
  "duckdb>=1.1",
  "numpy>=2.0",
  "scipy>=1.14",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3",
  "ruff>=0.6",
]

[project.scripts]
victory-trader = "victory_trader.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
