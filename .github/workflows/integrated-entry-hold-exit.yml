name: MoneyMaker Integrated Entry HOLD EXIT

on:
  push:
    branches:
      - main
    paths:
      - 'victory-trader/research/integrated-entry-hold-exit-request.json'

permissions:
  contents: read
  actions: read

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: victory-trader
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install MoneyMaker
        run: python -m pip install -e '.[dev]'
      - name: Test request 212
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q tests/test_integrated_entry_hold_exit.py
      - name: Download Request 171 rich-second phases
        uses: actions/download-artifact@v4
        with:
          pattern: request171-rich-second-*
          path: victory-trader/prepared/rich
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36033120106
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download Request 163 opportunity scan
        uses: actions/download-artifact@v4
        with:
          name: request163-opportunity-prepared
          path: victory-trader/prepared/opportunity
          repository: dayofhiki/MoneyMaker
          run-id: 36001945920
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download Request 178 fresh shards
        uses: actions/download-artifact@v4
        with:
          pattern: request178-fresh-*
          path: victory-trader/prepared/fresh
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36038233507
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Evaluate request 212
        run: |
          set -o pipefail
          mkdir -p artifacts
          python -m victory_trader.integrated_entry_hold_exit \
            --fit-positions prepared/rich/fit-rich-second.parquet \
            --calibration-positions prepared/rich/calibration-rich-second.parquet \
            --history-scan prepared/opportunity/opportunity-scan.parquet \
            --fresh-dir prepared/fresh \
            --output artifacts/integrated-entry-hold-exit-v12.json \
            --trajectories-output artifacts/integrated-entry-hold-exit-v12.parquet \
            | tee artifacts/integrated-entry-hold-exit-v12.log
      - uses: actions/upload-artifact@v4
        with:
          name: moneymaker-integrated-entry-hold-exit-v12-212
          path: |
            victory-trader/artifacts/integrated-entry-hold-exit-v12.json
            victory-trader/artifacts/integrated-entry-hold-exit-v12.parquet
            victory-trader/artifacts/integrated-entry-hold-exit-v12.log
          if-no-files-found: error
          retention-days: 90
