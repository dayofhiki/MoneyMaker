name: MoneyMaker Direct Action Advantage

on:
  push:
    branches:
      - research/direct-action-advantage-246
    paths:
      - 'victory-trader/research/direct-action-advantage-request.json'
      - 'victory-trader/src/victory_trader/direct_action_advantage.py'
      - 'victory-trader/tests/test_direct_action_advantage.py'
      - '.github/workflows/direct-action-advantage.yml'
  pull_request:
    paths:
      - 'victory-trader/research/direct-action-advantage-request.json'
      - 'victory-trader/src/victory_trader/direct_action_advantage.py'
      - 'victory-trader/tests/test_direct_action_advantage.py'
      - '.github/workflows/direct-action-advantage.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: victory-trader
    env:
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '2'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install frozen numerical stack
        run: python -m pip install -e '.[dev]' -c research/request214-requirements.txt
      - name: Check Request246 contracts
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q tests/test_direct_action_advantage.py tests/test_chronological_action_value.py
      - name: Download Request171 rich-second fit/calibration
        uses: actions/download-artifact@v4
        with:
          pattern: request171-rich-second-*
          path: victory-trader/prepared/rich
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36033120106
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Evaluate Request246
        run: |
          set -o pipefail
          mkdir -p artifacts
          git rev-parse HEAD > artifacts/source-sha.txt
          python -m victory_trader.direct_action_advantage \
            --fit-positions prepared/rich/fit-rich-second.parquet \
            --calibration-positions prepared/rich/calibration-rich-second.parquet \
            --output artifacts/request246.json \
            --rows-output artifacts/request246-rows.parquet \
            | tee artifacts/request246.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moneymaker-direct-action-advantage-246
          path: |
            victory-trader/artifacts/request246.json
            victory-trader/artifacts/request246-rows.parquet
            victory-trader/artifacts/request246.log
            victory-trader/artifacts/source-sha.txt
          if-no-files-found: warn
          retention-days: 90
