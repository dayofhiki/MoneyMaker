name: MoneyMaker Active Transport Capacity Frontier

on:
  push:
    branches:
      - main
    paths:
      - 'victory-trader/research/active-transport-capacity-frontier-request.json'

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: victory-trader
    env:
      MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
      MASSIVE_S3_ACCESS_KEY: ${{ secrets.MASSIVE_S3_ACCESS_KEY }}
      MASSIVE_S3_SECRET_KEY: ${{ secrets.MASSIVE_S3_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install MoneyMaker
        run: python -m pip install -e '.[dev]'

      - name: Test request 152
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q \
            tests/test_active_transport_capacity_frontier.py \
            tests/test_balanced_retention_active_allocator.py \
            tests/test_observation_transport.py \
            tests/test_actionable_active_handoff.py \
            tests/test_market_wide_focus_hazard.py

      - name: Restore market-wide Flat File cache
        uses: actions/cache/restore@v4
        with:
          path: victory-trader/data/cache/massive-flatfiles
          key: moneymaker-capacity-frontier-flatfiles-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            moneymaker-fresh-capacity-flatfiles-${{ runner.os }}-
            moneymaker-balanced-retention-flatfiles-${{ runner.os }}-
            moneymaker-retention-active-flatfiles-${{ runner.os }}-
            moneymaker-actionable-active-flatfiles-${{ runner.os }}-

      - name: Run request 152
        run: |
          set -o pipefail
          mkdir -p artifacts
          python -m victory_trader.active_transport_capacity_frontier \
            2026-01-02 2026-05-28 \
            --summary artifacts/active-transport-capacity-frontier-v10.json \
            | tee artifacts/active-transport-capacity-frontier-v10.log

      - name: Upload request 152 artifact
        uses: actions/upload-artifact@v4
        with:
          name: moneymaker-active-transport-capacity-frontier-v10-152
          path: |
            victory-trader/artifacts/active-transport-capacity-frontier-v10.json
            victory-trader/artifacts/active-transport-capacity-frontier-v10.log
          if-no-files-found: error
          retention-days: 90

      - name: Save market-wide Flat File cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: victory-trader/data/cache/massive-flatfiles
          key: moneymaker-capacity-frontier-flatfiles-${{ runner.os }}-${{ github.run_id }}
