name: MoneyMaker Multi Event Entry Continuation

on:
  push:
    branches:
      - research/multi-event-entry-continuation-227
    paths:
      - 'victory-trader/research/multi-event-entry-continuation-request.json'

permissions:
  contents: read
  actions: read

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: victory-trader
    env:
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '2'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install frozen numerical stack
        run: python -m pip install -e '.[dev]' -c research/request214-requirements.txt
      - name: Check code and multi-event entry contract
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q tests/test_multi_event_entry_continuation.py tests/test_pullback_turn_transition_entry.py
      - name: Download Request 171 rich-second fit/calibration
        uses: actions/download-artifact@v4
        with:
          pattern: request171-rich-second-*
          path: victory-trader/prepared/rich
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36033120106
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download Request 178 development shards
        uses: actions/download-artifact@v4
        with:
          pattern: request178-fresh-*
          path: victory-trader/prepared/fresh
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36038233507
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Evaluate request 227
        run: |
          set -o pipefail
          mkdir -p artifacts
          git rev-parse HEAD > artifacts/source-sha.txt
          python -m victory_trader.multi_event_entry_continuation \
            --fit-positions prepared/rich/fit-rich-second.parquet \
            --calibration-positions prepared/rich/calibration-rich-second.parquet \
            --fresh-dir prepared/fresh \
            --output artifacts/request227.json \
            --rows-output artifacts/request227-entry-policy.parquet \
            | tee artifacts/request227.log
      - uses: actions/upload-artifact@v4
        with:
          name: moneymaker-multi-event-entry-continuation-227
          path: |
            victory-trader/artifacts/request227.json
            victory-trader/artifacts/request227-entry-policy.parquet
            victory-trader/artifacts/request227.log
            victory-trader/artifacts/source-sha.txt
          if-no-files-found: error
          retention-days: 90
