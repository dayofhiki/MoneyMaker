name: MoneyMaker Actionable Active Transport

on:
  push:
    branches:
      - main
    paths:
      - 'victory-trader/research/actionable-active-transport-request.json'

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    defaults:
      run:
        working-directory: victory-trader
    env:
      MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
      MASSIVE_S3_ACCESS_KEY: ${{ secrets.MASSIVE_S3_ACCESS_KEY }}
      MASSIVE_S3_SECRET_KEY: ${{ secrets.MASSIVE_S3_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install MoneyMaker
        run: python -m pip install -e '.[dev]'

      - name: Test actionable transport
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q \
            tests/test_actionable_active_handoff.py \
            tests/test_active_transport_integrated_hierarchy.py \
            tests/test_market_wide_focus_hazard.py \
            tests/test_hierarchical_attention_runtime.py \
            tests/test_second_path_attention_probe.py

      - name: Restore market-wide Flat File cache
        uses: actions/cache/restore@v4
        with:
          path: victory-trader/data/cache/massive-flatfiles
          key: moneymaker-actionable-active-flatfiles-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            moneymaker-causal-population-flatfiles-${{ runner.os }}-
            moneymaker-position-observability-v11-flatfiles-${{ runner.os }}-
            moneymaker-hot-opportunity-flatfiles-${{ runner.os }}-

      - name: Restore one-second REST cache
        uses: actions/cache/restore@v4
        with:
          path: victory-trader/data/cache/massive-second-attention
          key: moneymaker-actionable-active-second-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            moneymaker-causal-population-second-${{ runner.os }}-
            moneymaker-position-observability-v11-second-${{ runner.os }}-
            moneymaker-hot-opportunity-second-${{ runner.os }}-

      - name: Run request 147 actionable active transport
        run: |
          set -o pipefail
          mkdir -p artifacts
          python -m victory_trader.actionable_active_handoff \
            2026-01-02 2026-05-13 \
            --output artifacts/actionable-active-transport-v10.parquet \
            --summary artifacts/actionable-active-transport-v10.json \
            | tee artifacts/actionable-active-transport-v10.log

      - name: Upload request 147 artifact
        uses: actions/upload-artifact@v4
        with:
          name: moneymaker-actionable-active-transport-v10-147
          path: |
            victory-trader/artifacts/actionable-active-transport-v10.parquet
            victory-trader/artifacts/actionable-active-transport-v10.json
            victory-trader/artifacts/actionable-active-transport-v10.log
          if-no-files-found: error
          retention-days: 90

      - name: Save one-second REST cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: victory-trader/data/cache/massive-second-attention
          key: moneymaker-actionable-active-second-${{ runner.os }}-${{ github.run_id }}

      - name: Save market-wide Flat File cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: victory-trader/data/cache/massive-flatfiles
          key: moneymaker-actionable-active-flatfiles-${{ runner.os }}-${{ github.run_id }}
