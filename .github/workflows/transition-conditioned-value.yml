name: MoneyMaker Transition Conditioned Value

on:
  push:
    branches:
      - research/transition-conditioned-value-221
    paths:
      - 'victory-trader/research/transition-conditioned-value-request.json'

permissions:
  contents: read
  actions: read

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: victory-trader
    env:
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '2'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install frozen numerical stack
        run: python -m pip install -e '.[dev]' -c research/request214-requirements.txt
      - name: Check code and transition-phase contract
        run: |
          ruff check src tests --select E4,E7,E9,F
          pytest -q tests/test_transition_conditioned_option_value.py tests/test_entry_conditioned_rich_second.py tests/test_multi_event_option_value.py
      - name: Download Request 171 rich-second fit/calibration
        uses: actions/download-artifact@v4
        with:
          pattern: request171-rich-second-*
          path: victory-trader/prepared/rich
          merge-multiple: true
          repository: dayofhiki/MoneyMaker
          run-id: 36033120106
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download Request 163 opportunity scan
        uses: actions/download-artifact@v4
        with:
          name: request163-opportunity-prepared
          path: victory-trader/prepared/opportunity
          repository: dayofhiki/MoneyMaker
          run-id: 36001945920
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download Request 220 scored fresh states
        uses: actions/download-artifact@v4
        with:
          name: moneymaker-entry-conditioned-rich-second-220
          path: victory-trader/prepared/request220
          repository: dayofhiki/MoneyMaker
          run-id: 36235273868
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Evaluate request 221
        run: |
          set -o pipefail
          mkdir -p artifacts
          git rev-parse HEAD > artifacts/source-sha.txt
          python -m victory_trader.transition_conditioned_option_value \
            --fit-positions prepared/rich/fit-rich-second.parquet \
            --calibration-positions prepared/rich/calibration-rich-second.parquet \
            --history-scan prepared/opportunity/opportunity-scan.parquet \
            --fresh-states prepared/request220/request220-states.parquet \
            --output artifacts/request221.json \
            --states-output artifacts/request221-states.parquet \
            | tee artifacts/request221.log
      - uses: actions/upload-artifact@v4
        with:
          name: moneymaker-transition-conditioned-value-221
          path: |
            victory-trader/artifacts/request221.json
            victory-trader/artifacts/request221-states.parquet
            victory-trader/artifacts/request221.log
            victory-trader/artifacts/source-sha.txt
          if-no-files-found: error
          retention-days: 90
